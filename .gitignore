# Build output. `cargo` rebuilds it; nothing here belongs to the project.
/target

# IntelliJ's own view of the project, which is per-machine.
/.idea
*.iml

# The notebook's kernel. Per-machine, and `sizing/model.py` needs nothing in it:
# `python3 sizing/model.py` runs on the standard library alone.
/.venv
